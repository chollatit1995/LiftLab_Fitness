# Supabase (liftlab-db) — auto-filled when connected via Vercel Storage
POSTGRES_URL=

# Alternative names (optional)
DATABASE_URL=
SUPABASE_DATABASE_URL=

# Supabase API (from Vercel Storage integration)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Auth (JWT session secret — required in production)
AUTH_SECRET=
